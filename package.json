{
  "name": "uigen",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm install && npx prisma generate && prisma migrate dev",
    "db:reset": "npx prisma migrate reset --force"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "next": "^15.5.15"
  },
  "devDependencies": {
    "eslint-config-next": "15.3.4",
    "prisma": "^6.10.1"
  }
}
